# - Find the Razor-qt include and library dirs and define a some macros
#


#find_package(Qt4 REQUIRED QUIET)
#include(${QT_USE_FILE})

#set(XDGQT_QT_LIBRARIES ${QT_LIBRARIES})

include_directories(${XDGQT_INCLUDE_DIRS})

